def two_basic_blocks():
    """An example of code with a two basic blocks. The body of the code
    and one for execption-branching exit.
    """
    return 5


def if_else_blocks(x):
    """An example of code with a two basic blocks. The body of the code
    and one for execption-branching exit.
    """
    return 1 if x else 5
